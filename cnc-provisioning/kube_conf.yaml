apiVersion: v1
kind: Config
clusters:
- name: "sl-p01"
  cluster:
    server: "https://rancher.condenser.arc.ucl.ac.uk/k8s/clusters/c-m-bv9x5ngh"

users:
- name: "sl-p01"
  user:
    token: ""


contexts:
- name: "sl-p01"
  context:
    user: "sl-p01"
    cluster: "sl-p01"

current-context: "sl-p01"
